# e2e working directory: talos secrets, machine configs, kubeconfig, downloaded images
/.e2e/
/bin/
/dist/
# `go build ./cmd/...` drops binaries in the repo root.
/paas-operator

# Never commit a kubeconfig or a talosconfig — both carry cluster-admin credentials.
kubeconfig
talosconfig
*.pem
*.key
coverage.out
